package Vetores;
/*
public class Prova2 {
    Letra A)


    int value = 10;

switch (value % 2) {
        case 0:
            System.out.println("Par");
            break;
        default:
            System.out.println("Impar");
    }
// A variável value não foi declarada e nem inicializada;
// A instrução break não foi declarado no case;
// Erro de lógica na saída de valores do Sistema;
// Foi corrigida a lógica do switch case incluido, o default para caso contrario.



    Letra B)

    int i;

for (i = 20; i > 2; i--) {
        i-=1;
        System.out.println(i);
    }

// A variável i não foi declarada;
// O teste booleano corrigido para maior que 2, impedindo que o 1 sejá incluido;
// Correção do incremento para Decremento, já que se trata da sequência ser decrescente;
// Incluido um novo decremento i-=1, para que ele mesmo valendo 20 decremente imprimindo só os ímpares.



    Letra C)

    public static void main (String[]args){

        int counter = 0;


        do{
            counter +=2;
            System.out.println(counter);

        } while(counter < 100);

    }

//Não declarou um tipo para a variável couter;
//Erro de sintaxe no comando while;
//Iniciar a variável counter com o valor 0 para que o código inicie corretamente;



    Letra D)



    public class Array {
        public static void main(String[] args) {

            int[] b = new int[11];

            for (int i = 1; i < b.length; i++) {

                b[i] = i;

                System.out.println(b[i]);
            }
        }
    }

// O método Void não retorna Nada;
// alteração no tamanho de posições do vetor de 10 para 11;
// Correção da inicialização da variável i = 0 para i = 1;
// Alterado o teste booleano de <= para <;
// Atribuição da variavel i ao vetor, para retornar o Array com os elementos.
// Mudança da saída do Sistema de return para System.out.print(b[i]);

*/